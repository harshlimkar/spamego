name: scam_detect_call
description: Real-time VoIP calling app with live AI scam detection.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.1.5
  http: ^1.2.1
  web_socket_channel: ^3.0.1
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
